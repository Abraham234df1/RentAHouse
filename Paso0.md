Crea un proyecto ASP.NET Core MVC en C# llamado RentaDepartamentosWeb,
usando .NET 8. Genera la estructura de carpetas: Controllers, Models, Data,
Repositories, Services, Views/Departamentos. Configura el archivo
Program.cs con inyección de dependencias básica y soporte para
Microsoft.Data.SqlClient. No implementes lógica todavía, solo el
esqueleto del proyecto y el archivo .csproj con las dependencias.